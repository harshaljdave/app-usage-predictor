## These are sample models trained on synthetic data and very small in size.

To generate personalised models collect data for your device and run following command:
```bash
  python training/train_all.py --db usage.db --output outputs/models
```